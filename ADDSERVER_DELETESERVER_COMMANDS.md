# Команды управления серверами Outline

## /addserver - Добавление нового сервера

Команда для добавления нового Outline VPN сервера в бот (только для администратора).

### Процесс добавления (4 шага):

1. **Шаг 1/4**: Введите название страны на английском
   - Примеры: `germany`, `france`, `usa`, `kazakhstan`
   - Используется как ключ в конфигурации

2. **Шаг 2/4**: Введите название страны на русском
   - Примеры: `Германия`, `Франция`, `США`, `Казахстан`
   - Отображается пользователям в меню выбора региона
   - Автоматически добавляется флаг страны (если доступен)

3. **Шаг 3/4**: Введите API URL сервера Outline
   - Формат: `https://123.456.789.012:12345/aBcDeFgH`
   - Должен начинаться с `https://`

4. **Шаг 4/4**: Введите SHA256 сертификат
   - 64-символьная hex-строка
   - Пример: `a1b2c3d4e5f6...` (64 символа)

### Формат флагов

Флаги стран хранятся в Unicode escape последовательностях:
- 🇰🇿 Казахстан → `\uD83C\uDDF0\uD83C\uDDFF`
- 🇳🇱 Нидерланды → `\uD83C\uDDF3\uD83C\uDDF1`
- 🇩🇪 Германия → `\uD83C\uDDE9\uD83C\uDDEA`
- 🇫🇷 Франция → `\uD83C\uDDEB\uD83C\uDDF7`
- 🇺🇸 США → `\uD83C\uDDFA\uD83C\uDDF8`

### Результат

После успешного добавления сервер будет сохранен в `core/api_s/outline/settings_api_outline.json` в формате:

```json
{
  "kazakhstan": {
    "name_en": "kazakhstan",
    "name_ru": "🇰🇿 Казахстан",
    "api_url": "https://...",
    "cert_sha256": "...",
    "is_active": true
  }
}
```

⚠️ **Требуется перезапуск бота** для применения изменений.

---

## /deleteserver - Удаление сервера

Команда для удаления Outline VPN сервера из бота (только для администратора).

### Процесс удаления:

1. **Выбор сервера**: Отображается список всех активных серверов
   - Показывается название (с флагом) и количество активных ключей
   - Пример: `🇰🇿 Казахстан (15 ключей)`

2. **Подтверждение**: После выбора сервера показывается:
   - Полная информация о сервере
   - Количество активных ключей
   - Предупреждение о необратимости действия
   - Кнопки: "✅ Да, удалить" и "❌ Отмена"

3. **Выполнение удаления**:
   - Удаляются все ключи пользователей с этого сервера из Outline VPN
   - Обновляется статус premium всех пользователей → `False`
   - Сервер удаляется из `settings_api_outline.json`
   - Выводится статистика: количество удаленных ключей и ошибок

### Что происходит при удалении:

1. ✅ Все ключи удаляются из Outline VPN через API
2. ✅ У всех пользователей на этом сервере отключается подписка
3. ✅ Конфигурация сервера удаляется из бота
4. ⚠️ Данные о ключах остаются в БД (для истории), но становятся неактивными

### Безопасность:

- Только администратор может выполнять эту команду
- Двухэтапное подтверждение (выбор + подтверждение)
- Логирование всех действий
- Обработка ошибок при недоступности Outline API

⚠️ **Требуется перезапуск бота** после удаления сервера.

---

## Технические детали

### Флаги стран (COUNTRY_FLAGS)

Поддерживаются флаги для 30+ стран:
- Европа: nederland, germany, france, spain, italy, poland, uk, sweden, norway, finland, switzerland, austria, belgium, czech, denmark, ireland, portugal, romania, ukraine
- Азия: japan, singapore, india, turkey, uae
- Америка: usa, canada, brazil
- Австралия: australia
- СНГ: kazakhstan

Если флаг не найден, используется глобус: 🌐 (`\uD83C\uDF10`)

### Структура данных в БД

**Таблица Users**:
- `region_server` (str) - название сервера (ключ из JSON)
- `outline_id` (str) - ID ключа в Outline VPN
- `premium` (bool) - статус активной подписки

При удалении сервера:
- `premium` → `False`
- `outline_id` остается (для истории)
- `region_server` остается (для истории)

### Обработка ошибок

- ✅ Валидация URL (должен начинаться с https://)
- ✅ Валидация сертификата (64 hex символа)
- ✅ Проверка прав администратора
- ✅ Обработка недоступности Outline API
- ✅ Логирование всех операций

---

## Примеры использования

### Добавление сервера:

```
Администратор: /addserver

Бот: 🌍 Добавление нового Outline сервера
     Шаг 1/4: Введите название страны на английском
     (например: germany, france, usa, kazakhstan)

Администратор: kazakhstan

Бот: ✅ Страна (EN): kazakhstan
     ✅ Флаг: 🇰🇿
     Шаг 2/4: Введите название страны на РУССКОМ языке
     (например: Германия, Франция, США, Казахстан)

Администратор: Казахстан

Бот: ✅ Страна (EN): kazakhstan
     ✅ Страна (RU): Казахстан
     ✅ Флаг: 🇰🇿
     Шаг 3/4: Введите API URL сервера Outline
     (например: https://123.456.789.012:12345/aBcDeFgH)

[...продолжение диалога...]
```

### Удаление сервера:

```
Администратор: /deleteserver

Бот: 🗑️ Удаление сервера
     Выберите сервер для удаления...
     [Кнопки: 🇰🇿 Казахстан (15 ключей)]
     
Администратор: [нажимает на кнопку]

Бот: ⚠️ Подтверждение удаления сервера
     Сервер: 🇰🇿 Казахстан
     Активных ключей: 15
     ...
     [Кнопки: ✅ Да, удалить | ❌ Отмена]

Администратор: [нажимает "Да, удалить"]

Бот: ⏳ Удаление сервера... Это может занять некоторое время.

Бот: ✅ Сервер удален
     Сервер: 🇰🇿 Казахстан
     Удалено ключей: 15
     ⚠️ Для применения изменений требуется перезапуск бота
```

---

## Отмена операции

В любой момент процесса можно отправить `/cancel` для отмены операции.
