# Быстрая настройка новых функций бота техподдержки

## Шаг 1: Настройка переменных окружения

Добавьте в файл `.env` (или `core/TEMP.env` для локальной разработки):

```bash
# Username основного бота (без символа @)
MAIN_BOT_USERNAME=OneYearVpb_bot
```

**Где взять username основного бота?**
- Откройте Telegram и найдите вашего основного бота
- Username отображается в профиле бота (например: @OneYearVpb_bot)
- Используйте только имя без @: `OneYearVpb_bot`

## Шаг 2: Перезапуск бота

После добавления переменной окружения перезапустите бот техподдержки:

```bash
# Остановите бот (Ctrl+C если запущен вручную)

# Или через systemd:
sudo systemctl restart olvpn-support-bot

# Проверьте статус:
sudo systemctl status olvpn-support-bot
```

## Шаг 3: Проверка работы

1. **Отправьте тестовое сообщение** в бот техподдержки от имени тестового пользователя
2. Администратор должен увидеть сообщение с **4 кнопками**:
   - ↩️ Ответить
   - 📜 История
   - 🎁 Выдать промо
   - 🔄 Заменить ключ

## Использование

### Выдача промо-ключа

1. Пользователь пишет в техподдержку
2. Администратор нажимает "🎁 Выдать промо"
3. Пользователь получает уведомление:
   ```
   🎁 Вам выдан тестовый доступ к демо-среде!
   
   📍 Регион сервера: 🇳🇱 Нидерланды
   ⏰ Действует до: 27.12.2025 - 15:30
   
   Перейдите в основной бот и используйте команду /start
   ```

### Замена ключа

1. Пользователь жалуется на проблемы с сервером
2. Администратор нажимает "🔄 Заменить ключ"
3. Пользователь получает уведомление:
   ```
   🔄 Ваш доступ был заменен!
   
   Старый сервер: 🇳🇱 Нидерланды
   Новый сервер: 🇩🇪 Германия
   
   Перейдите в основной бот и используйте команду /start
   ```

## Что делать при ошибках

### Ошибка: "Пользователь не найден в БД"
- Пользователь должен сначала запустить основной бот (`/start`)
- Только после этого его можно будет найти в базе данных

### Ошибка: "Нет активных ключей для замены"
- У пользователя нет действующих ключей
- Вместо замены используйте "🎁 Выдать промо"

### Ошибка: "Нет доступных серверов"
- Проверьте, что в `core/api_s/outline/settings_api_outline.json` есть активные серверы
- Должно быть минимум 2 активных сервера для замены

## Проверка логов

Логи бота техподдержки сохраняются в стандартный вывод Python:

```bash
# Если запущен через systemd:
sudo journalctl -u olvpn-support-bot -f

# Ищите записи:
# - "Admin {id} gave promo to user {id}"
# - "Admin {id} replaced key for user {id}"
```

## Дополнительная информация

Подробная документация: [SUPPORT_BOT_ADMIN_FEATURES.md](SUPPORT_BOT_ADMIN_FEATURES.md)
