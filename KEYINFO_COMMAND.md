## Новая команда администратора: /keyinfo

### Описание
Команда `/keyinfo <user_id>` позволяет администратору просмотреть полную информацию о ключе доступа пользователя, включая трафик, и при необходимости заблокировать ключ.

### Использование

```
/keyinfo 123456789
```

где `123456789` - это ID пользователя в Telegram.

### Информация, которую выводит команда
- **Пользователь** - Telegram имя пользователя и его ID
- **Регион** - Регион сервера, на котором размещена подписка
- **Трафик использован** - Количество трафика в ГБ за последние 30 дней
- **Статус** - Активна ли подписка (✅ Активен/❌ Неактивен)

### Кнопка блокировки ключа

После вывода информации администратору предоставляется кнопка "🔒 Заблокировать ключ"

При нажатии на кнопку:
1. Ключ удаляется из Outline VPN сервера
2. Статус `premium` пользователя выставляется на `False`
3. Пользователю автоматически отправляется уведомление об окончании подписки

### Примеры

**Успешный просмотр информации:**
```
/keyinfo 987654321

📊 Информация о ключе

Пользователь: John_Doe (ID: 987654321)
Регион: nederland
Трафик использован: 12.45 ГБ
Статус: ✅ Активен

[Кнопка: 🔒 Заблокировать ключ]
```

**Ошибки:**
- `❌ Пользователь с ID XXX не найден в БД` - пользователь не был зарегистрирован в боте
- `❌ У пользователя XXX нет активного ключа` - у пользователя нет действующей подписки
- `❌ Ключ пользователя XXX не найден на сервере` - ключ есть в БД, но отсутствует на Outline сервере

### Технические детали

**Где была добавлена функция:**
- `core/handlers/key_info.py` - основной обработчик команды `/keyinfo`
- `core/handlers/handlers_keyboards/admin_block_key_handler.py` - обработчик блокировки ключа
- `core/handlers/handler_keyboard.py` - добавлен маршрут для callback'ов блокировки
- `core/bot.py` - зарегистрирована команда `/keyinfo`

**Как это работает:**
1. Команда проверяет, что пользователь является администратором (по ID из `ADMIN_TLG`)
2. Извлекает ID пользователя из параметра команды
3. Ищет пользователя в БД по Telegram ID
4. Получает информацию о ключе из Outline VPN (включая статистику трафика)
5. Отображает информацию и кнопку блокировки
6. При нажатии на кнопку удаляет ключ и отправляет уведомление

### API Outline, используемый в функции

Функция использует методы класса `OutlineManager`:
- `get_key_from_ol(id_user)` - получить объект ключа с информацией о трафике
  - Возвращает объект `OutlineKey` с полем `used_bytes` (трафик в байтах за 30 дней)
  - Трафик автоматически конвертируется в ГБ: `used_gb = used_bytes / (1024**3)`

- `delete_key_from_ol(id_user)` - удалить ключ из Outline

### Возможные улучшения

1. **Подтверждение перед блокировкой** - добавить дополнительное диалоговое окно "Вы уверены?"
2. **История блокировок** - записывать в БД информацию о заблокированных ключах
3. **Причина блокировки** - позволять администратору указать причину блокировки в уведомлении
4. **Массовая блокировка** - блокировать все истекшие ключи по команде
