# Видимость Ёмкости Серверов для Пользователей

## Описание

Реализована функциональность отображения текущей загруженности серверов VPN при выборе региона. Пользователи теперь видят, сколько ключей используется на каждом сервере и максимальное количество доступных ключей.

## Функциональность

### 1. Отображение Ёмкости в Меню Выбора Региона

Каждый регион в меню выбора теперь показывает:
- **Индикатор статуса**: 
  - 🟢 — сервер доступен (есть свободные ключи)
  - 🔴 — сервер недоступен (лимит достигнут)
- **Информацию об использовании**: `(текущее_количество/максимум)`
- **Пример**: "🟢 🇳🇱 Нидерланды (45/100)"

### 2. Недоступные Серверы

Если лимит ключей на сервере достигнут:
- Кнопка региона остаётся видимой в меню
- При нажатии на кнопку показывается сообщение об недоступности
- Сообщение содержит текущее состояние: "❌ Лимит достигнут (100/100)"
- Пользователю предлагается выбрать другой регион

### 3. Защита при Покупке

Даже если пользователь пытается купить ключ на недоступном сервере (например, через устаревший кэш или состояние), система:
1. Проверяет лимит перед обработкой платежа
2. Показывает сообщение об ошибке
3. Возвращает меню с актуальной информацией об ёмкости

## Технические Изменения

### Файлы Изменены

#### 1. [core/keyboards/choise_region_button.py](core/keyboards/choise_region_button.py)
**Изменения:**
- Функция `choise_region_keyboard()` сделана асинхронной
- Добавлен импорт `check_server_key_limit` для получения информации об ёмкости
- Функция `create_region_button_from_json()` теперь асинхронная
- Для каждого активного сервера:
  - Запрашивается текущее количество ключей
  - Вычисляется индикатор (🟢/🔴)
  - Формируется текст кнопки с информацией об использовании
  - Кнопке присваивается атрибут `is_available`

**Пример:**
```python
async def choise_region_keyboard() -> InlineKeyboardMarkup:
    keyboard_builder = InlineKeyboardBuilder()
    region_buttons = await create_region_button_from_json()
    for button in region_buttons:
        keyboard_builder.add(
            InlineKeyboardButton(
                text=button['text'],
                callback_data=button['callback_data']
            )
        )
    # ...
    return keyboard_builder.as_markup()
```

#### 2. [core/handlers/handlers_keyboards/choise_region.py](core/handlers/handlers_keyboards/choise_region.py)
**Изменения:**
- Функция `region_handler()` теперь обрабатывает недоступные серверы
- Проверяет префикс `disabled_` в callback данных
- Для недоступных серверов:
  - Извлекает название региона
  - Получает информацию об ёмкости
  - Показывает сообщение об ошибке с текущим использованием
  - Повторно отправляет меню выбора региона

**Пример:**
```python
if region_data.startswith("disabled_"):
    region_name = region_data.replace("disabled_", "")
    is_available, current_keys, max_keys = await check_server_key_limit(region_name)
    content = f"❌ Сервер недоступен\n\nИспользовано ключей: {current_keys}/{max_keys}"
    return content, await choise_region_keyboard()
```

#### 3. [core/handlers/handlers_keyboards/get_key_handler.py](core/handlers/handlers_keyboards/get_key_handler.py)
**Изменения:**
- Все обработчики (`day_key()`, `week_key()`, `month_key()`, `my_key()`) теперь используют `await choise_region_keyboard()`
- Добавлена проверка лимита перед обработкой платежа
- При достижении лимита показывается сообщение с информацией об ёмкости

**Пример:**
```python
is_available, current_keys, max_keys = await check_server_key_limit(region_server)
if not is_available:
    content = f"❌ Сервер недоступен\n\nДостигнут лимит ключей ({current_keys}/{max_keys})"
    return content, await choise_region_keyboard()
```

## Информация об Ёмкости

Информация об ёмкости берётся из:

1. **Конфигурация**: [core/api_s/outline/settings_api_outline.json](core/api_s/outline/settings_api_outline.json)
   - Поле `max_keys` содержит максимальное количество ключей на сервере
   - По умолчанию: 100 ключей

2. **Текущее использование**: Запрашивается с сервера Outline VPN через `OutlineManager.get_keys()`
   - Позволяет получить актуальное количество выданных ключей

## Пользовательский Опыт

### Сценарий 1: Доступный Сервер
```
Пользователь видит: "🟢 🇳🇱 Нидерланды (45/100)"
После нажатия: Переход к выбору срока подписки
```

### Сценарий 2: Недоступный Сервер
```
Пользователь видит: "🔴 🇩🇪 Германия (100/100)"
После нажатия: "❌ Сервер недоступен\n\nИспользовано ключей: 100/100"
              Предложение выбрать другой регион
```

### Сценарий 3: Попытка Покупить на Недоступном Сервере
```
Система проверяет лимит перед платежом
Показывает: "❌ Сервер недоступен\n\nДостигнут лимит ключей (100/100)"
            Меню с актуальной информацией об ёмкости
```

## Администраторские Команды

### Просмотр Активных Ключей
```
/activekeys
```
Показывает список всех пользователей с активными подписками и информацией об их истечении.

### Информация о Ключе Пользователя
```
/keyinfo <user_id>
```
Показывает информацию о ключе пользователя, включая:
- Регион сервера
- Трафик за 30 дней
- Статус активности
- Кнопку для блокировки ключа

### Добавление Нового Сервера
```
/addserver
```
Интерактивный диалог для добавления нового VPN-сервера с поддержкой:
- Выбора страны (автоматическое определение флага)
- Ввода API URL сервера
- Ввода SHA256 сертификата
- Установки максимального количества ключей (`max_keys`)

## Синтаксическая Корректность

Все файлы проверены на наличие синтаксических ошибок:
- ✅ [core/keyboards/choise_region_button.py](core/keyboards/choise_region_button.py) — ошибок не найдено
- ✅ [core/handlers/handlers_keyboards/choise_region.py](core/handlers/handlers_keyboards/choise_region.py) — ошибок не найдено
- ✅ [core/handlers/handlers_keyboards/get_key_handler.py](core/handlers/handlers_keyboards/get_key_handler.py) — ошибок не найдено

## Заметки Разработчика

1. **Асинхронность**: Все функции получения клавиатур теперь асинхронные из-за необходимости запроса информации с сервера Outline.

2. **Кэширование**: Информацию об ёмкости следует кэшировать, если запросы на Outline VPN медленные.

3. **Отключенные серверы**: Недоступные серверы остаются видимыми в меню для информационных целей, но не позволяют выбрать платный тариф.

4. **Бэкапы**: При добавлении нового сервера убедитесь, что установлено значение `max_keys` в [settings_api_outline.json](core/api_s/outline/settings_api_outline.json).

## Версионирование

- **Версия**: 1.0
- **Дата обновления**: Декабрь 2025
- **Статус**: Готово к использованию
