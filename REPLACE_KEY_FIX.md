# Исправление функции замены ключа

## Проблема
При попытке заменить ключ пользователя через команду `/keyinfo` и кнопку "🔁 Заменить ключ" возникала ошибка Telegram API: "message not modified".

## Причина
При редактировании сообщений с помощью `callback.message.edit_text()`, которые изначально содержали inline-клавиатуру, необходимо явно указывать параметр `reply_markup=None`, иначе Telegram API возвращает ошибку.

## Решение
Добавлен параметр `reply_markup=None` ко всем вызовам `callback.message.edit_text()` в файле `core/handlers/replace_key.py`:

### Список исправленных мест:

1. **Строка ~52** - Ошибка "Ключ не найден в базе данных"
2. **Строка ~67** - Ошибка "Пользователь не найден в базе данных"
3. **Строка ~76** - Прогресс "Замена ключа..."
4. **Строка ~88** - Ошибка "Нет доступных серверов"
5. **Строка ~147** - Ошибка при создании нового ключа
6. **Строка ~183** - Успешное завершение замены

## Дополнительные улучшения

### 1. Улучшенное логирование
Добавлено детальное логирование всех этапов замены ключа:
- Запрос на замену с `short_id` и ID администратора
- Общее количество ключей в БД
- Найденный целевой ключ (ID, пользователь, сервер)
- Создание нового ключа с параметрами
- Успешное создание и сохранение

### 2. Отображение названий серверов
Используется функция `get_server_display_name()` для отображения названий серверов с флагами стран:
- В сообщении о прогрессе: "Старый сервер: 🇳🇱 Нидерланды"
- В сообщении администратору: показывает старый и новый сервер с флагами
- В уведомлении пользователю: "Новый сервер: 🇩🇪 Германия"

### 3. Исправлен формат даты
Дата истечения форматируется в правильный формат для `add_user_key()`:
```python
date_str = expiry_date.strftime('%d.%m.%Y - %H:%M')
```

## Тестирование

Для тестирования используйте:

1. Создайте тестового пользователя: `/seed`
2. Просмотрите информацию о ключах: `/keyinfo test_<id>`
3. Нажмите кнопку "🔁 Заменить ключ 1"
4. Проверьте:
   - Отсутствие ошибок Telegram API
   - Удаление старого ключа с сервера и из БД
   - Создание нового ключа на другом сервере
   - Уведомление пользователя с новым ключом
   - Обновление меню "Мой ключ"

5. Очистите тестовые данные: `/unseed`

## Связанные файлы

- `core/handlers/replace_key.py` - Основной обработчик замены ключа
- `core/handlers/key_info.py` - Команда `/keyinfo` с кнопками замены
- `core/bot.py` - Регистрация callback-обработчика `rpl_key_*`
- `core/api_s/outline/outline_api.py` - Функция `get_server_display_name()`

## Дата исправления
Декабрь 2025
