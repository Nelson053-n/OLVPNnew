# Интеграция нейросети в бот техподдержки (Roadmap)

## 🎯 Цель

Добавить возможность автоматических ответов на типовые вопросы с использованием нейросети, при этом сохранив возможность вмешательства администратора.

## 🏗️ Архитектура (Планируемая)

```
Пользователь → Support Bot → [1. Анализ нейросетью] → [2. Решение]
                                                              │
                                                    ┌─────────┴─────────┐
                                                    ↓                   ↓
                                            Типовой вопрос    Сложный вопрос
                                                    │                   │
                                                    ↓                   ↓
                                          Автоответ от AI      Администратор
                                          (с кнопками:         (как сейчас)
                                           "Помогло" / 
                                           "Связаться с 
                                            оператором")
```

## 📋 Этапы реализации

### Этап 1: Подготовка данных

1. **Сбор типовых вопросов:**
   - Анализ существующих обращений
   - Категоризация по темам
   - Создание базы знаний

2. **Структура базы знаний:**
   ```json
   {
     "faq": [
       {
         "category": "connection",
         "keywords": ["не работает", "подключение", "не могу подключиться"],
         "question": "Как подключиться к VPN?",
         "answer": "Для подключения выполните следующие шаги:\n1. Установите Outline Client...",
         "confidence_threshold": 0.7
       },
       {
         "category": "setup",
         "keywords": ["установка", "настройка", "как установить"],
         "question": "Как установить приложение?",
         "answer": "Скачайте Outline Client...",
         "confidence_threshold": 0.8
       }
     ]
   }
   ```

### Этап 2: Выбор нейросети

#### Вариант 1: OpenAI GPT (Рекомендуется)

**Преимущества:**
- Высокое качество ответов
- Готовый API
- Поддержка контекста

**Настройка:**
```python
import openai

openai.api_key = os.getenv("OPENAI_API_KEY")

async def get_ai_response(user_message: str, user_history: list = None):
    """Получение ответа от GPT"""
    messages = [
        {
            "role": "system",
            "content": """Ты — ассистент службы поддержки Outline Solutions.
            Компания предоставляет B2B решения для организации частных сетей.
            НЕ предоставляем потребительские VPN-услуги.
            
            Отвечай кратко и по делу. Если вопрос не по теме, вежливо перенаправь."""
        }
    ]
    
    # Добавляем историю диалога
    if user_history:
        messages.extend(user_history)
    
    messages.append({"role": "user", "content": user_message})
    
    response = await openai.ChatCompletion.acreate(
        model="gpt-4",
        messages=messages,
        temperature=0.7,
        max_tokens=500
    )
    
    return response.choices[0].message.content
```

#### Вариант 2: Локальная модель (LangChain + Llama)

**Преимущества:**
- Без зависимости от внешних API
- Бесплатно
- Контроль над данными

**Настройка:**
```python
from langchain import LLMChain
from langchain.llms import LlamaCpp
from langchain.prompts import PromptTemplate

# Инициализация модели
llm = LlamaCpp(
    model_path="./models/llama-2-7b-chat.gguf",
    temperature=0.7,
    max_tokens=500
)

prompt = PromptTemplate(
    input_variables=["question"],
    template="""Ты — помощник службы поддержки Outline Solutions.
    
    Вопрос пользователя: {question}
    
    Твой ответ:"""
)

chain = LLMChain(llm=llm, prompt=prompt)

async def get_ai_response(user_message: str):
    return await chain.arun(question=user_message)
```

### Этап 3: Модификация support_bot.py

#### 3.1. Добавление конфигурации

```python
# В начало файла
AI_ENABLED = os.getenv("AI_ENABLED", "false").lower() == "true"
AI_CONFIDENCE_THRESHOLD = float(os.getenv("AI_CONFIDENCE_THRESHOLD", "0.75"))
ADMIN_APPROVAL_REQUIRED = os.getenv("ADMIN_APPROVAL_REQUIRED", "true").lower() == "true"
```

#### 3.2. Новая функция для анализа

```python
async def analyze_and_respond(message: Message, user_id: int):
    """Анализ сообщения и решение о типе ответа"""
    
    if not AI_ENABLED:
        # AI отключен - обычная пересылка администратору
        return await forward_to_admin(message)
    
    user_message = message.text
    
    # 1. Попытка получить ответ от AI
    try:
        ai_response = await get_ai_response(user_message)
        confidence = await calculate_confidence(user_message, ai_response)
        
        # 2. Если уверенность высокая
        if confidence >= AI_CONFIDENCE_THRESHOLD:
            
            if ADMIN_APPROVAL_REQUIRED:
                # Отправляем администратору на утверждение
                await send_for_approval(message, ai_response, confidence)
            else:
                # Отправляем пользователю сразу
                await send_ai_response(message, ai_response)
        else:
            # Низкая уверенность - передаём администратору
            await forward_to_admin(message, note="AI не уверен в ответе")
            
    except Exception as e:
        logger.error(f"Ошибка AI: {e}")
        # При ошибке - обычная пересылка
        await forward_to_admin(message)
```

#### 3.3. Отправка ответа с кнопками

```python
from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton

async def send_ai_response(message: Message, ai_response: str):
    """Отправка AI ответа пользователю с кнопками обратной связи"""
    
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [
            InlineKeyboardButton(
                text="✅ Помогло",
                callback_data=f"ai_helpful_{message.from_user.id}"
            ),
            InlineKeyboardButton(
                text="❌ Не помогло",
                callback_data=f"ai_not_helpful_{message.from_user.id}"
            )
        ],
        [
            InlineKeyboardButton(
                text="👤 Связаться с оператором",
                callback_data=f"ai_escalate_{message.from_user.id}"
            )
        ]
    ])
    
    await message.answer(
        f"🤖 <b>Автоматический ответ:</b>\n\n{ai_response}",
        reply_markup=keyboard,
        parse_mode=ParseMode.HTML
    )
    
    # Уведомляем администратора
    await send_notification_to_admin(
        f"🤖 AI ответил пользователю {message.from_user.full_name} (ID: {message.from_user.id})\n\n"
        f"Вопрос: {message.text}\n\n"
        f"Ответ: {ai_response}"
    )
```

#### 3.4. Обработка callback'ов

```python
@router.callback_query(F.data.startswith("ai_"))
async def handle_ai_feedback(callback: CallbackQuery):
    """Обработка обратной связи по AI ответам"""
    
    action = callback.data.split("_")[1]
    user_id = int(callback.data.split("_")[2])
    
    if action == "helpful":
        await callback.message.edit_text(
            f"{callback.message.text}\n\n✅ <i>Ответ был полезен</i>",
            parse_mode=ParseMode.HTML
        )
        await callback.answer("Спасибо за обратную связь!")
        
        # Уведомляем администратора
        await send_notification_to_admin(
            f"✅ Пользователь {user_id} подтвердил полезность AI ответа"
        )
        
    elif action == "not_helpful":
        await callback.message.edit_text(
            f"{callback.message.text}\n\n❌ <i>Ожидайте ответа оператора</i>",
            parse_mode=ParseMode.HTML
        )
        await callback.answer("Ваш запрос передан оператору")
        
        # Передаём администратору
        await forward_to_admin_with_context(user_id, callback.message.text)
        
    elif action == "escalate":
        await callback.answer("Соединяем с оператором...")
        await callback.message.edit_text(
            f"{callback.message.text}\n\n👤 <i>Передано оператору</i>",
            parse_mode=ParseMode.HTML
        )
        
        # Передаём администратору
        await forward_to_admin_with_context(user_id, callback.message.text)
```

### Этап 4: База знаний (knowledge_base.json)

```json
{
  "version": "1.0",
  "disclaimer": "Outline Solutions — B2B платформа, не предоставляем потребительские VPN",
  "categories": {
    "connection": {
      "name": "Проблемы с подключением",
      "questions": [
        {
          "id": "conn_001",
          "keywords": ["не работает", "подключение", "не могу подключиться", "ошибка подключения"],
          "answer": "Проверьте следующее:\n1. Установлен ли Outline Client\n2. Правильно ли скопирован ключ\n3. Нет ли блокировки в вашей сети\n\nЕсли проблема сохраняется, свяжитесь с оператором."
        }
      ]
    },
    "setup": {
      "name": "Установка и настройка",
      "questions": [
        {
          "id": "setup_001",
          "keywords": ["установка", "как установить", "настройка", "скачать"],
          "answer": "Для установки Outline Client:\n\n1. Windows/Mac: outline.com/get-started\n2. Android: Google Play\n3. iOS: App Store\n\nПосле установки добавьте ключ доступа."
        }
      ]
    },
    "billing": {
      "name": "Вопросы по оплате",
      "questions": [
        {
          "id": "bill_001",
          "keywords": ["оплата", "платёж", "стоимость", "цена", "продлить"],
          "answer": "Для вопросов по оплате свяжитесь с оператором — нажмите кнопку ниже."
        }
      ]
    }
  },
  "fallback_response": "Извините, я не могу ответить на этот вопрос. Соединяю вас с оператором."
}
```

### Этап 5: Переменные окружения

```env
# AI Configuration
AI_ENABLED=true
AI_PROVIDER=openai  # или llama
OPENAI_API_KEY=sk-...
AI_CONFIDENCE_THRESHOLD=0.75
ADMIN_APPROVAL_REQUIRED=false

# AI Model Settings
AI_MODEL=gpt-4
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=500
```

## 🧪 Тестирование

### Тестовые сценарии

1. **Типовой вопрос:**
   ```
   Пользователь: "Как установить приложение?"
   → AI отвечает автоматически
   → Кнопки: Помогло / Не помогло / Оператор
   ```

2. **Сложный вопрос:**
   ```
   Пользователь: "У меня специфическая проблема с корпоративным фаерволом"
   → AI определяет низкую уверенность
   → Передаёт администратору
   ```

3. **Эскалация:**
   ```
   Пользователь: "Как установить?"
   → AI отвечает
   → Пользователь: "Не помогло"
   → Передаётся администратору с контекстом
   ```

### Метрики для оценки

- **Процент успешных AI ответов** (кнопка "Помогло")
- **Процент эскалаций** (передача оператору)
- **Время ответа** (AI vs человек)
- **Удовлетворённость пользователей**

## 📊 Постепенное внедрение

### Фаза 1: Тестирование (1-2 недели)
- AI только анализирует, не отвечает
- Администратор видит предложенные AI ответы
- Сбор статистики точности

### Фаза 2: Утверждение администратором (2-4 недели)
- AI предлагает ответы
- Администратор утверждает перед отправкой
- Обучение на утверждённых ответах

### Фаза 3: Автоматические ответы (постоянно)
- AI отвечает самостоятельно на типовые вопросы
- Администратор получает уведомления
- Пользователь может вызвать оператора

## 🔒 Безопасность

1. **Ограничение контекста:**
   - AI не имеет доступа к внутренним данным
   - Только публичная информация

2. **Модерация:**
   - Логирование всех AI ответов
   - Проверка администратором (опционально)

3. **Фильтрация:**
   - Запрещённые темы
   - Проверка на вредоносный контент

## 💰 Стоимость

### OpenAI GPT-4
- ~$0.03 за 1000 токенов (входящие)
- ~$0.06 за 1000 токенов (исходящие)
- Примерно $0.10 за диалог

### Локальная модель (Llama)
- Бесплатно
- Требуется сервер с GPU (опционально)
- Качество ниже GPT-4

## 📝 Пример итогового кода

См. файл `support_bot_with_ai.py` (будет создан при реализации)

## ✅ Чеклист готовности

- [ ] База знаний создана и заполнена
- [ ] Выбрана и настроена нейросеть
- [ ] Реализованы функции AI анализа
- [ ] Добавлены кнопки обратной связи
- [ ] Настроена эскалация к администратору
- [ ] Реализовано логирование
- [ ] Проведено тестирование
- [ ] Собрана статистика эффективности

## 🚀 Следующие шаги

1. **Анализ существующих обращений** → Определение типовых вопросов
2. **Создание базы знаний** → knowledge_base.json
3. **Выбор AI провайдера** → OpenAI или локальная модель
4. **Прототип** → Тестирование на ограниченной аудитории
5. **Полное внедрение** → Постепенное увеличение охвата

---

**Статус:** 📋 Планируется  
**Приоритет:** Средний  
**Дата:** После стабилизации базовой версии
