# Обновление бота техподдержки - Умный выбор сервера

## Дата: 20 декабря 2025

## Что исправлено

### 1. ✅ Исправлен username основного бота
- **Было:** `OutlineVPNBot` (неверный)
- **Стало:** `OneYearVpb_bot` (правильный)
- **Где обновлено:**
  - `core/TEMP.env` - шаблон конфигурации
  - `support_bot.py` - fallback значение
  - Вся документация (3 файла)

### 2. ✅ Добавлен умный выбор сервера

#### Выдача промо-доступа
**Старая логика:**
- Использовался текущий регион пользователя
- Или дефолтный сервер "nederland"
- Не учитывалась нагрузка серверов

**Новая логика:**
1. Получает список всех ключей пользователя
2. Определяет на каких серверах уже есть ключи
3. Исключает эти серверы из выбора
4. Подсчитывает активные ключи на оставшихся серверах
5. Выбирает сервер с **минимальной нагрузкой**
6. Если на всех серверах уже есть ключи - использует все серверы

#### Замена ключа
**Старая логика:**
- Выбирался первый доступный сервер (кроме текущего)
- Не учитывалась нагрузка

**Новая логика:**
1. Исключает текущий сервер пользователя
2. Подсчитывает активные ключи на всех оставшихся серверах
3. Выбирает сервер с **минимальной нагрузкой**

## Новая функция

### `get_least_busy_server_for_user(user_id: int) -> str`

Умная функция выбора сервера:

```python
async def get_least_busy_server_for_user(user_id: int) -> str:
    """
    Получить самый свободный сервер для пользователя.
    Исключает серверы, на которых у пользователя уже есть ключи.
    Возвращает сервер с минимальным количеством ключей.
    """
```

**Алгоритм:**
1. Получает все ключи пользователя → `user_servers`
2. Получает все активные серверы → `all_servers`
3. Фильтрует: `available_servers = all_servers - user_servers`
4. Для каждого доступного сервера считает активные ключи
5. Возвращает сервер с минимальным `count`
6. При ошибке возвращает `'nederland'` (fallback)

**Логирование:**
```
User {id} has keys on servers: {set}
Available servers for user {id}: {list}
Server {name} has {count} active keys
Selected least busy server: {name} (load: {count})
```

## Преимущества

### 🎯 Для пользователей
- Получают доступ к **менее загруженным серверам**
- Распределение по разным серверам автоматически
- Нет дублирования ключей на одном сервере

### ⚖️ Для инфраструктуры
- **Балансировка нагрузки** между серверами
- Равномерное распределение пользователей
- Предотвращение перегрузки одного сервера

### 🔧 Для администратора
- Автоматическое управление нагрузкой
- Подробное логирование для анализа
- Гибкость: работает с любым количеством серверов

## Примеры работы

### Сценарий 1: Новый пользователь
```
Серверы:
- nederland: 100 ключей
- germany: 80 ключей ✓ (самый свободный)
- france: 120 ключей

У пользователя: нет ключей

Результат: выбран germany (80 ключей)
```

### Сценарий 2: У пользователя уже есть ключ
```
Серверы:
- nederland: 100 ключей (есть ключ у пользователя)
- germany: 80 ключей ✓ (самый свободный без ключа)
- france: 70 ключей (есть ключ у пользователя)

У пользователя: ключи на nederland, france

Результат: выбран germany (единственный без ключа)
```

### Сценарий 3: На всех серверах есть ключи
```
Серверы:
- nederland: 100 ключей (есть ключ)
- germany: 80 ключей ✓ (самый свободный)
- france: 120 ключей (есть ключ)

У пользователя: ключи на всех серверах

Результат: выбран germany (самый свободный из всех)
```

## Замена ключа

### Пример
```
Текущий сервер пользователя: nederland (100 ключей)

Доступные серверы:
- germany: 80 ключей ✓ (самый свободный)
- france: 120 ключей

Результат: ключ заменен на germany
```

## Обновленные файлы

### Код
- ✅ `support_bot.py` - добавлена функция `get_least_busy_server_for_user()`
- ✅ `support_bot.py` - обновлена логика в `callback_give_promo()`
- ✅ `support_bot.py` - обновлена логика в `callback_replace_key()`
- ✅ `support_bot.py` - исправлен fallback username на `OneYearVpb_bot`

### Конфигурация
- ✅ `core/TEMP.env` - обновлен `MAIN_BOT_USERNAME=OneYearVpb_bot`

### Документация
- ✅ `SUPPORT_BOT_ADMIN_FEATURES.md` - описана новая логика
- ✅ `SUPPORT_BOT_ADMIN_QUICKSTART.md` - обновлен username
- ✅ `SUPPORT_BOT_README.md` - обновлена информация

## Как применить обновление

### 1. Перезапустите бот техподдержки
```bash
sudo systemctl restart olvpn-support-bot
```

### 2. Проверьте логи
```bash
sudo journalctl -u olvpn-support-bot -f
```

### 3. Ищите в логах
```
Selected least busy server for user {id}: {server} (load: {count})
```

## Тестирование

### Тест 1: Выдача промо нескольким пользователям
1. Отправьте сообщения в техподдержку от 3 разных пользователей
2. Выдайте каждому промо через кнопку "🎁 Выдать промо"
3. Проверьте логи - ключи должны распределиться по разным серверам

### Тест 2: Замена ключа
1. Пользователь с ключом пишет в техподдержку
2. Нажмите "🔄 Заменить ключ"
3. Проверьте, что выбран сервер с минимальной нагрузкой

### Тест 3: Ссылка на основной бот
1. Выдайте промо или замените ключ
2. Проверьте уведомление пользователю
3. Ссылка должна быть: `https://t.me/OneYearVpb_bot`

## Совместимость

- ✅ Обратная совместимость с существующими ключами
- ✅ Работает с любым количеством серверов (1+)
- ✅ Fallback на `nederland` при ошибках
- ✅ Не требует миграции данных

## Метрики для мониторинга

Следите за:
1. Распределением ключей по серверам
2. Логами выбора серверов
3. Балансом нагрузки между серверами

Команды для проверки:
```bash
# Статистика по серверам
/serverstats

# Логи выбора сервера
sudo journalctl -u olvpn-support-bot | grep "Selected least busy server"
```

---

**Автор:** GitHub Copilot  
**Дата:** 20 декабря 2025  
**Версия:** 2.0
