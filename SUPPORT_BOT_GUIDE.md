# Бот техподдержки для Outline VPN

## Описание

Отдельный Telegram-бот для обработки обращений в техподдержку. Все сообщения от пользователей автоматически пересылаются администратору, который может отвечать через команду `/reply`.

## Возможности

✅ **Автоматическая пересылка сообщений** администратору
✅ **Ответы через команду** `/reply USER_ID текст`
✅ **Предзаполненный дисклеймер** при открытии чата через основного бота
✅ **Поддержка только текстовых сообщений** (медиафайлы отклоняются)
✅ **Логирование** всех операций

## Установка и настройка

### 1. Создайте нового бота в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Рекомендуемое имя: `YourProject Support Bot`

### 2. Добавьте переменные окружения

В файл `.env` (или `core/TEMP.env` для разработки) добавьте:

```env
# Токен бота техподдержки (обязательно для запуска support_bot.py)
SUPPORT_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Username бота для кнопки в основном боте (без символа @)
SUPPORT_CHAT_USERNAME=helpvpb_bot
```

**Важно:** 
- `SUPPORT_BOT_TOKEN` - токен **нового бота** (не основного)
- `SUPPORT_CHAT_USERNAME` - username **нового бота** (тот же бот)

### 3. Запустите бота техподдержки

#### Вручную (для тестирования)

```bash
cd /workspaces/OLVPNnew
python support_bot.py
```

#### Через systemd (production)

**Шаг 1: Создайте файл сервиса**

```bash
sudo nano /etc/systemd/system/olvpn-support-bot.service
```

Вставьте следующее содержимое (замените пути на ваши):

```ini
[Unit]
Description=OLVPN Support Bot - Telegram Support Service
After=network.target

[Service]
Type=simple
User=root
Group=root
WorkingDirectory=/root/OLVPNnew
Environment="PATH=/root/OLVPNnew/venv/bin:/usr/local/bin:/usr/bin:/bin"
ExecStart=/root/OLVPNnew/venv/bin/python3 /root/OLVPNnew/support_bot.py
Restart=always
RestartSec=10

# Логирование
StandardOutput=journal
StandardError=journal
SyslogIdentifier=olvpn-support-bot

# Безопасность
NoNewPrivileges=true
PrivateTmp=true

[Install]
WantedBy=multi-user.target
```

**Важно:** Замените `/root/OLVPNnew` на ваш реальный путь к проекту.

**Шаг 2: Перезагрузите systemd и запустите сервис**

```bash
# Перезагрузить конфигурацию systemd
sudo systemctl daemon-reload

# Включить автозапуск при старте системы
sudo systemctl enable olvpn-support-bot

# Запустить сервис
sudo systemctl start olvpn-support-bot

# Проверить статус
sudo systemctl status olvpn-support-bot
```

**Шаг 3: Проверьте работу**

```bash
# Просмотр логов в реальном времени
sudo journalctl -u olvpn-support-bot -f

# Последние 50 строк логов
sudo journalctl -u olvpn-support-bot -n 50

# Перезапуск сервиса
sudo systemctl restart olvpn-support-bot

# Остановка сервиса
sudo systemctl stop olvpn-support-bot
```

## Использование

### Для пользователей

1. Открывают основного бота → Мои подключения
2. Нажимают кнопку "💬 Техподдержка"
3. Открывается чат с ботом поддержки с предзаполненным дисклеймером:
   > "Здравствуйте. Мы не предоставляем потребительские VPN-услуги..."
4. Пользователь отправляет своё сообщение
5. Получает подтверждение: "✅ Ваше сообщение получено..."

### Для администратора

#### Получение сообщений

Администратор получает уведомление в формате:

```
📩 Новое сообщение в техподдержку

👤 От: Иван Иванов
🆔 User ID: 123456789
📧 Username: @ivan_ivanov

💬 Сообщение:
Не работает подключение к серверу

Чтобы ответить, используйте команду:
/reply 123456789 ваш_ответ
```

#### Ответ пользователю

```
/reply 123456789 Проверьте настройки ключа в приложении
```

Пользователь получит:

```
📬 Ответ от службы поддержки:

Проверьте настройки ключа в приложении
```

## Команды бота

### Для пользователей

- `/start` - Приветственное сообщение с дисклеймером
- Любой текст - Отправить сообщение в поддержку

### Для администратора

- `/reply USER_ID текст` - Ответить пользователю

**Пример:**
```
/reply 123456789 Спасибо за обращение! Проблема решена.
```

## Архитектура

### Файл: `support_bot.py`

**Основные функции:**

1. **`cmd_start()`** - Обработчик `/start`, приветствие с дисклеймером
2. **`forward_to_admin()`** - Пересылка сообщений от пользователей администратору
3. **`reply_to_user()`** - Отправка ответов от администратора пользователю
4. **`handle_media()`** - Отклонение медиафайлов с пояснением

### Словарь `user_mapping`

Хранит информацию о пользователях для удобного ответа:

```python
{
  123456789: {
    'username': 'ivan_ivanov',
    'full_name': 'Иван Иванов'
  }
}
```

### Логирование

Все операции записываются в консоль с форматом:

```
2025-12-19 14:30:45 - __main__ - INFO - Сообщение от пользователя 123456789 переслано администратору
```

## Интеграция с основным ботом

### Кнопка техподдержки

В функции `my_key()` файла `core/handlers/handlers_keyboards/get_key_handler.py`:

```python
from core.settings import support_chat_username
from urllib.parse import quote

disclaimer_text = (
    "Здравствуйте. Мы не предоставляем потребительские VPN-услуги. "
    "Outline Solutions — это B2B-платформа для организации частных сетей. "
    "Если вы хотите использовать нашу инфраструктуру для законных бизнес-задач "
    "(удаленный доступ к вашему серверу, защита разработки), мы готовы обсудить."
)

encoded_text = quote(disclaimer_text)
support_url = f"https://t.me/{support_chat_username}?text={encoded_text}"

kb.row(InlineKeyboardButton(text='💬 Техподдержка', url=support_url))
```

## Безопасность

⚠️ **Важные моменты:**

1. **Токен бота** должен храниться только в `.env` и **никогда** не коммититься в Git
2. **Проверка администратора** - команда `/reply` доступна только `admin_tlg`
3. **Валидация USER_ID** - проверка на числовой формат перед отправкой
4. **Обработка ошибок** - все исключения логируются и обрабатываются

## Мониторинг

### Проверка статуса сервиса

```bash
sudo systemctl status olvpn-support-bot
```

### Просмотр логов

```bash
sudo journalctl -u olvpn-support-bot -f
```

### Перезапуск при изменениях

```bash
sudo systemctl restart olvpn-support-bot
```

## Ограничения

- ❌ Не поддерживаются медиафайлы (фото, видео, документы)
- ❌ Нет истории диалогов (каждое сообщение независимо)
- ❌ Один администратор (расширяется добавлением в список)

## Расширение функционала

### Добавление нескольких администраторов

В `support_bot.py` замените:

```python
if message.from_user.id != int(admin_tlg):
```

На:

```python
ADMIN_IDS = [int(admin_tlg), 987654321, 123456789]  # Добавьте ID администраторов
if message.from_user.id not in ADMIN_IDS:
```

### Поддержка медиафайлов

Замените функцию `handle_media()` на пересылку файлов администратору:

```python
@router.message(F.photo | F.video | F.document)
async def forward_media_to_admin(message: Message):
    await bot.copy_message(
        chat_id=int(admin_tlg),
        from_chat_id=message.chat.id,
        message_id=message.message_id
    )
```

## Troubleshooting

### Бот не запускается

1. Проверьте наличие `SUPPORT_BOT_TOKEN` в `.env`
2. Проверьте корректность токена через [@BotFather](https://t.me/BotFather)
3. Убедитесь, что порты не заблокированы файрволом

### Сообщения не доходят до администратора

1. Проверьте значение `ADMIN_TLG` в `.env`
2. Убедитесь, что администратор запустил `/start` у бота техподдержки
3. Проверьте логи: `journalctl -u olvpn-support-bot -n 50`

### Ответы не доходят до пользователей

1. Проверьте формат команды: `/reply USER_ID текст` (с пробелами)
2. Убедитесь, что USER_ID корректный (скопируйте из сообщения)
3. Проверьте, что пользователь не заблокировал бота

---

**Дата создания:** 19 декабря 2025  
**Связанные файлы:** `support_bot.py`, `core/settings.py`, `core/handlers/handlers_keyboards/get_key_handler.py`
