# Настройка кнопки техподдержки

## Обзор изменений

Добавлена кнопка "💬 Техподдержка" в меню "Мои доступы", которая открывает чат с ботом поддержки с предзаполненным текстом-дисклеймером.

**Важно:** Теперь техподдержка реализована через отдельного Telegram-бота, который автоматически пересылает сообщения администратору.

## Файлы изменены

1. **`core/settings.py`** - добавлена переменная окружения `support_chat_username`
2. **`core/TEMP.env`** - добавлен пример переменной `SUPPORT_CHAT_USERNAME`
3. **`core/handlers/handlers_keyboards/get_key_handler.py`** - функция `my_key()` обновлена с кнопкой техподдержки

## Настройка

### 1. Добавьте переменные окружения

В файле `.env` (или `core/TEMP.env` для локальной разработки) добавьте:

```env
# Username бота техподдержки (для кнопки в основном боте)
SUPPORT_CHAT_USERNAME=your_support_bot

# Токен бота техподдержки (для запуска отдельного бота)
SUPPORT_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

**Важно:**
- Создайте нового бота через [@BotFather](https://t.me/BotFather)
- `SUPPORT_CHAT_USERNAME` - username бота (без символа @)
- `SUPPORT_BOT_TOKEN` - токен нового бота
- Подробная инструкция: [SUPPORT_BOT_GUIDE.md](SUPPORT_BOT_GUIDE.md)

### 2. Запустите бота техподдержки

**Вручную (для тестирования):**
```bash
python support_bot.py
```

**Через systemd (production):**
См. полную инструкцию в [SUPPORT_BOT_GUIDE.md](SUPPORT_BOT_GUIDE.md#установка-и-настройка)

### 3. Перезапустите основного бота

После добавления переменных окружения перезапустите основного бота:

```bash
# Если используется systemd
sudo systemctl restart olvpn-bot

# Или остановите и запустите вручную
python main.py
```

## Как работает кнопка

1. Пользователь открывает меню "Мои доступы"
2. Видит кнопку "💬 Техподдержка" 
3. При нажатии открывается чат с ботом техподдержки
4. В поле ввода автоматически вставляется текст:

> Здравствуйте. Мы не предоставляем потребительские VPN-услуги. Outline Solutions — это B2B-платформа для организации частных сетей. Если вы хотите использовать нашу инфраструктуру для законных бизнес-задач (удаленный доступ к вашему серверу, защита разработки), мы готовы обсудить.

5. Пользователь отправляет сообщение
6. Сообщение **автоматически пересылается администратору**
7. Администратор отвечает через команду `/reply USER_ID текст`
8. Ответ приходит пользователю от бота техподдержки

## Бот техподдержки

Создан отдельный бот (`support_bot.py`), который:
- ✅ Пересылает все сообщения администратору из `admin_tlg`
- ✅ Позволяет администратору отвечать через `/reply`
- ✅ Показывает информацию о пользователе (ID, username, имя)
- ✅ Отклоняет медиафайлы с пояснением

**Подробная документация:** [SUPPORT_BOT_GUIDE.md](SUPPORT_BOT_GUIDE.md)

## Технические детали

### Формат URL

Кнопка использует Telegram Deep Link формата:
```
https://t.me/{username}?text={encoded_disclaimer}
```

### Кодирование текста

Текст дисклеймера кодируется с помощью `urllib.parse.quote()` для корректной передачи в URL.

### Fallback значение

Если переменная `SUPPORT_CHAT_USERNAME` не установлена, используется значение по умолчанию: `your_support_bot`.

⚠️ **Важно:** Обязательно установите правильное значение в production, иначе кнопка будет вести на несуществующего бота.

## Расположение кнопки

Кнопка "💬 Техподдержка" расположена **после всех кнопок управления ключами**, но **перед кнопкой "🔙 Назад"**.

Пример структуры клавиатуры:
```
[📋 Копировать 1] [🗑️ Удалить 1]
[🔄 Заменить 1]
[💬 Техподдержка]
[🔙 Назад]
```

## Изменение текста дисклеймера

Чтобы изменить текст, который отправляется при открытии чата поддержки, отредактируйте переменную `disclaimer_text` в файле:

**`core/handlers/handlers_keyboards/get_key_handler.py`**, функция `my_key()`:

```python
disclaimer_text = (
    "Ваш новый текст дисклеймера здесь..."
)
```

---

**Дата создания:** 19 декабря 2025  
**Связанные файлы:** `core/settings.py`, `core/handlers/handlers_keyboards/get_key_handler.py`
