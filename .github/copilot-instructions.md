# Инструкции Copilot для Бота Outline VPN

## Обзор проекта
Это **Telegram-бот** для покупки ключей доступа Outline VPN с поддержкой нескольких серверов. Бот интегрируется с:
- **Telegram API** (aiogram 3+) для взаимодействия с пользователем
- **Outline VPN** (кастомный модуль в `outline_vpn/`) для управления ключами доступа
- **YooKassa** платежная система для обработки платежей
- **SQLite + SQLAlchemy 2+** для хранения данных пользователей и платежей
- **Jinja2 шаблоны** в `core/templates/` для кастомизируемых ответов

**Ключевые особенности**:
- Мультирегиональная поддержка серверов с динамическим выбором
- Система промо-ключей для бесплатного пробного периода (7 дней)
- Административные команды для управления серверами и ценами без перезапуска
- Система блокировки с историей и причинами

## Архитектура и поток данных

### Многопроцессная архитектура
Бот запускает **два параллельных процесса** через multiprocessing в `main.py`:
1. **Процесс бота** (`run_bot()`) - Обрабатывает взаимодействие с пользователем Telegram
2. **Процесс проверки** (`run_checker()`) - Запускает `check_time_subscribe.py` для мониторинга и истечения подписок

Оба процесса работают с независимыми async event loop'ами.

### Основной поток: Платёж пользователя → Генерация ключа
1. Пользователь вызывает `/start` → `core/handlers/start.py` проверяет БД и Outline на наличие существующих ключей
2. Пользователь выбирает длительность (день/неделя/месяц) через inline-кнопки
3. Платёж создаётся через `core/api_s/api_youkassa/youkassa_api.py`
4. После успешного платежа `OutlineManager` генерирует новый ключ в `core/api_s/outline/outline_api.py`
5. Ключ сохраняется в таблице `Users` с датой истечения
6. Фоновый процесс периодически вызывает `check_time_subscribe()` для отзыва истекших ключей

### Схема базы данных (SQLite)
**Основная таблица Users** (`core/sql/base.py`):
  - `account` (int, unique) - ID пользователя Telegram
  - `key` (str) - **DEPRECATED**: Старый формат с одним ключом
  - `date` (DateTime) - Дата истечения подписки
  - `premium` (bool) - Статус активности
  - `region_server` (str) - Выбранный регион сервера
  - `promo_key` (bool) - Флаг использования бесплатного пробного периода
  - `account_name` (str) - Telegram username

**Новая система ключей UserKey** (мультиключевая):
  - `account` (FK to Users) - Связь с пользователем
  - `access_url` (str) - URL доступа Outline
  - `outline_id` (str) - ID ключа в Outline API
  - `region_server` (str) - Регион сервера
  - `premium` (bool) - Статус активности ключа
  - `date` (DateTime) - Дата истечения
  - `promo` (bool) - Промо-ключ?
  - `created_at` (DateTime) - Время создания

**Другие таблицы**:
- **UserPay** - Записи о платежах с внешним ключом на Users
- **BlockHistory** - История блокировок с причинами и метаданными

## Ключевые файлы и паттерны

### Паттерн обработчиков (`core/handlers/`)
- **Callback-маршрутизация**: `build_and_edit_message()` в `handler_keyboard.py` маршрутизирует все нажатия inline-кнопок через `switch_dict`
- **FSMContext** используется для многошаговых команд администратора (`/addserver`, `/deleteserver`, `/editprice`)
- Отдельные обработчики в `handlers_keyboards/` (например `get_key_handler.py`) возвращают кортежи `(text, reply_markup)`
- **Пример flow**: Кнопки День/Неделя/Месяц → создание платежа → показ ссылки на оплату
- **Специальные callback'и**: Некоторые callback'и (например `give_promo_`, `confirm_block_key_`) обрабатываются напрямую в `handler_keyboard.py` с извлечением ID из префикса

### Использование шаблонов (Jinja2)
- Шаблоны в `core/templates/` (HTML-файлы) рендерятся динамически
- `create_answer_from_html()` в `core/utils/create_view.py` загружает и рендерит шаблоны
- Шаблоны позволяют обновлять ответы без перезагрузки бота
- Все ответы пользователям идут через шаблоны - избегайте хардкода текста в обработчиках

### Интеграция Outline (`core/api_s/outline/`)
- Класс `OutlineManager` обёртывает кастомный модуль `outline_vpn.OutlineVPN`
- **Поддержка нескольких регионов**: `settings_api_outline.json` хранит API URL'ы и сертификаты для нескольких серверов
- Методы: `get_key_from_ol()`, `create_key_in_ol()`, `delete_key_from_ol()`
- Формат ключа: структурированные URL доступа с встроенной аутентификацией

### Операции с базой данных (`core/sql/`)
- Использует **SQLAlchemy ORM** с синхронными сессиями
- Все функции БД помечены как `async def` но используют **блокирующий синхронный код** (вызовы Session())
  - Это сделано намеренно, но может быть улучшено с помощью `asyncpg` + SQLAlchemy async
- Функции называются с префиксом контекста: `get_user_data_from_table_users()`, `set_key_to_table_users()`

### Интеграция платежей (`core/api_s/api_youkassa/`)
- `create_payment()` возвращает URL для оплаты
- `check_payment()` проверяет статус платежа в YooKassa
- `get_user_payments()` ищет исторические платежи по ID Telegram (встроен в описание)

## Конфигурация и секреты

### Переменные окружения (`.env`)
- `API_KEY_TLG` - Токен Telegram-бота
- `ADMIN_TLG` - ID администратора
- `YOUKASSA_ID` - ID торговца YooKassa
- `YOUKASSA_SECRET` - Секретный ключ YooKassa
- Загружаются через `python-dotenv` в `core/settings.py`
- **ВАЖНО**: В production бот падает с ошибкой если переменные отсутствуют (безопасность)
- Fallback: Если `.env` пустой, проверяется `core/TEMP.env` (для локальной разработки)

### Конфигурация сервера Outline (`core/api_s/outline/settings_api_outline.json`)
```json
{
  "nederland": {
    "api_url": "https://...",
    "cert_sha256": "...",
    "is_active": true,
    "name_en": "Nederland",
    "name_ru": "🇳🇱 Нидерланды"
  }
}
```
- **Динамическое управление**: Используйте `/addserver` и `/deleteserver` для изменения
- Флаги стран определяются в `add_server.py` через словарь `COUNTRY_FLAGS`

### Конфигурация цен (`core/settings_prices.json`)
```json
{
  "day": {"amount": 7, "days": 1, "word_days": "день"},
  "week": {"amount": 40, "days": 7, "word_days": "дней"},
  "month": {"amount": 150, "days": 30, "word_days": "дней"},
  "promo": {"days": 7, "word_days": "дней"}
}
```
- **Динамическое управление**: Используйте `/editprice` для изменения без перезапуска
- Загружается через `load_prices()` в `edit_price.py` и `build_pay.py`

## Общие задачи разработки

### Добавление новой кнопки/обработчика
1. Создайте функцию обработчика в `core/handlers/handlers_keyboards/` возвращающую `(str, InlineKeyboardMarkup)`
2. Добавьте case в `switch_dict` в `handler_keyboard.py`
3. Создайте кнопку клавиатуры в `core/keyboards/` с соответствующими данными callback'а

### Изменение текста ответа
1. Отредактируйте шаблон в `core/templates/` (перезагрузка бота не требуется)
2. Или создайте новый обработчик с кастомным текстом

### Добавление нового региона/сервера
**Через команду (рекомендуется):**
1. `/addserver` - интерактивный процесс с 4 шагами через FSM
2. Вводите: страну (кнопки), русское название, API URL, сертификат SHA256
3. Флаг страны добавляется автоматически из словаря `COUNTRY_FLAGS`
4. **Требуется перезапуск бота** для активации

**Вручную:**
1. Добавьте запись в `settings_api_outline.json` с учётными данными API
2. При необходимости обновите логику в `get_name_all_active_server_ol()`
3. Регион автоматически появится в меню выбора региона

### Отладка проблем с платежами
1. Команда администратора `/findpay <user_id>` ищет платежи пользователя в YooKassa
2. Проверьте статус вручную в панели YooKassa
3. Примечание: Поиск платежей зависит от ID пользователя в описании платежа

### Тестирование с тестовыми данными
**Создание тестового пользователя:**
- `/seed` - создает пользователя `test_XXXXX` с 2 ключами:
  - 1 просроченный (истёк вчера)
  - 1 активный (истекает через 1-3 часа)
- Ключи создаются на **реальном Outline сервере**
- Платёж создаётся в БД, но НЕ через YooKassa API

**Удаление тестовых данных:**
- `/unseed` - удаляет всех пользователей с префиксом `test_*`
- Блокирует и удаляет их ключи с Outline серверов
- Полностью очищает БД от тестовых пользователей

### Управление ценами
- `/editprice` - интерактивное изменение цен (день/неделя/месяц)
- Изменения применяются **немедленно** без перезапуска
- Процесс: просмотр текущих цен → выбор периода → ввод новой цены → сохранение
- Файл: `core/settings_prices.json`

### Просмотр информации о ключе и его блокировка (администратор)
1. Команда `/keyinfo <user_id>` выводит информацию о ключе пользователя:
   - Telegram имя пользователя
   - Регион выбранного сервера
   - Трафик за 30 дней (в ГБ)
   - Статус активности ключа
2. Команда показывает кнопку "🔒 Заблокировать ключ" для администратора
3. При нажатии на кнопку:
   - Показывается подтверждение с опциями:
     - ✅ Да, заблокировать - немедленная блокировка
     - ✍️ Заблокировать с причиной - FSM для ввода причины
     - ❌ Отмена
   - Ключ удаляется из Outline VPN
   - Статус premium выставляется на False
   - Причина сохраняется в таблицу `BlockHistory`
   - Пользователю отправляется уведомление об окончании подписки

### Просмотр списка активных пользователей (администратор)
1. Команда `/activekeys` выводит список всех пользователей с активными подписками
2. Информация включает:
   - ID пользователя и Telegram имя
   - Регион сервера
   - Дата и время окончания подписки
   - Количество дней до окончания
3. Список отсортирован по дате окончания (ближайшие сначала)
4. Визуальные индикаторы:
   - 🔴 Ключ заканчивается завтра или сегодня
   - 🟡 Ключ заканчивается через 2-3 дня
   - 🟢 Ключ ещё действителен более 3 дней
5. Если пользователей больше 10, список выводится частями

### Массовая блокировка просроченных ключей
- `/massblock` - ручная проверка и блокировка всех просроченных ключей
- Возвращает количество заблокированных ключей
- Дублирует функциональность автоматического процесса `check_time_subscribe.py`
- Используется для принудительной синхронизации БД и Outline серверов

## Известные паттерны и анти-паттерны

### Намеренное асинхронное с блокировкой
Функции БД используют `async def` но синхронные сессии SQLAlchemy. Это работает, но не даёт преимуществ async. Рассмотрите миграцию на `asyncpg`, если БД станет узким местом.

### Пути к шаблонам жёстко закодированы
Шаблоны загружаются из статических путей вроде `core/templates/`. Нет конфигурации для директории шаблонов.

### Выбор региона через цикл
`start.py` проходит по всем регионам в поиске существующих ключей. Это значит, что пользователь со старым ключом найдёт его даже если добавлен в другой регион.

### Минимум админ-команд
Реализованы команды: `/start`, `/findpay`, `/get_log_pay`, `/get_db`, `/promo`, `/keyinfo`, `/activekeys`, `/massblock`, `/addserver`, `/deleteserver`, `/editprice`, `/seed`, `/unseed`, `/serverstats`, `/migrateserver`. Новые админ-функции требуют изменения кода.

### Замена ключей через callback
Функция замены ключа (`replace_key.py`) использует callback формата `rpl_key_<short_id>` где short_id - последние 8 символов UUID ключа. Полный поиск по БД для сопоставления.

### Новые возможности (Декабрь 2025)
- **`/serverstats`** - Мониторинг загрузки всех серверов (количество ключей, трафик)
- **`/migrateserver <from> <to>`** - Массовый автоматический перенос пользователей между серверами с уведомлениями
- **Исправлена замена ключей** - Новый ключ теперь корректно привязывается к пользователю (исправлен формат даты)

## Тестирование и развёртывание

### Локальное тестирование
```bash
pip install -r requirements.txt
python main.py  # Запросит секреты если .env отсутствует
```

### Production (пример systemd в README)
Использует systemd-сервис с `ExecStart` указывающим на venv Python и main.py. Убедитесь что .env находится в рабочей директории.

### Логирование
Два отдельных логгера через `RotatingFileLogger`:
- Логи основного бота → `logs/log_main.py` с конфигом `log_settings_base.json`
- Логи платежей → отдельный экземпляр с `log_settings_payments.json`

---

**Обновлено**: Декабрь 2025  
**Ключевая точка контакта**: Проверьте `core/bot.py` для настройки Dispatcher и `main.py` для многопроцессной архитектуры.
