# Миграция старых ключей в новую систему

## Проблема

При переходе со старой версии бота на новую возникает несовместимость схем хранения ключей:

- **Старая система**: Ключи хранились в поле `Users.key` (строка)
- **Новая система**: Ключи хранятся в таблице `UserKey` с полями:
  - `access_url` - URL доступа к VPN
  - `outline_id` - уникальный ID ключа в Outline
  - `region_server` - регион сервера
  - `premium` - статус активности
  - `date` - дата истечения
  - `promo` - флаг промо-ключа
  - `created_at` - дата создания

## Решение

Созданы две команды для безопасной миграции:

### 1. `/checkstatus` - Проверка статуса миграции

Показывает сколько пользователей нуждаются в миграции.

**Вывод:**
```
📊 Статус миграции

👥 Всего пользователей: 150

✅ Уже мигрированы: 120
⏳ Требуют миграции: 25
📭 Без старых ключей: 5

💡 Используйте /migrate для миграции оставшихся ключей
```

**Когда использовать:**
- Перед началом миграции (узнать масштаб)
- После миграции (проверить результат)
- Для мониторинга процесса

---

### 2. `/migrate` - Выполнение миграции

Автоматически переносит все старые ключи в новую систему.

**Что делает команда:**

1. ✅ Сканирует всех пользователей с заполненным `Users.key`
2. ✅ Проверяет наличие ключа на Outline сервере
3. ✅ Создает запись в `UserKey` с сохранением:
   - Даты истечения (`date`)
   - Статуса премиум (`premium`)
   - Региона сервера (`region_server`)
   - Флага промо (`promo_key`)
4. ✅ Пропускает уже мигрированных пользователей
5. ✅ **НЕ удаляет** старые данные из `Users.key` (для безопасности)

**Вывод:**
```
📊 Отчет о миграции ключей

👥 Всего пользователей в БД: 150
🔑 Пользователей со старыми ключами: 30

✅ Успешно мигрировано: 25
🔄 Уже были мигрированы: 3
❌ Ошибки миграции: 1
🔍 Ключей не найдено на сервере: 1

Детальный отчет:
✅ @user1 (ID: 123456): мигрирован на nederland
✅ @user2 (ID: 234567): мигрирован на germany
❌ @user3 (ID: 345678): ключ не найден на сервере nederland
...
```

---

## Пошаговая инструкция миграции

### Шаг 1: Подготовка

1. **Сделайте резервную копию БД:**
   ```bash
   cp database.db database.db.backup
   ```

2. **Проверьте статус:**
   ```
   /checkstatus
   ```

### Шаг 2: Запуск миграции

1. **Выполните команду:**
   ```
   /migrate
   ```

2. **Дождитесь завершения** - процесс может занять несколько минут при большой БД

3. **Изучите отчет** - проверьте наличие ошибок

### Шаг 3: Проверка результата

1. **Повторно проверьте статус:**
   ```
   /checkstatus
   ```
   Поле "⏳ Требуют миграции" должно быть 0 или содержать только проблемные ключи

2. **Проверьте работу ключей:**
   - Зайдите под тестовым аккаунтом
   - Выполните `/start`
   - Проверьте отображение ключа

### Шаг 4: Обработка ошибок

**Если ключ не найден на сервере:**
- Возможно, сервер был изменен
- Или ключ был удален вручную
- **Решение**: Выдайте новый ключ через `/promo` или пусть пользователь купит

**Если ошибка миграции:**
- Проверьте логи в `logs/log_main.py`
- Найдите строки с `[MIGRATION]`
- Исправьте проблему и повторите `/migrate` (дубли не создадутся)

---

## Технические детали

### Логика поиска ключа на сервере

Команда использует несколько стратегий для поиска старого ключа:

1. **По старому ID** из `Users.key` (если там хранился `outline_id`)
2. **По account ID** - пытается найти ключ с именем = `user.account`
3. **Пропускает**, если ключ не найден ни одним способом

### Безопасность данных

- ✅ Старые данные **НЕ удаляются** из `Users.key`
- ✅ При повторном запуске миграция **не создаст дубликаты**
- ✅ Используется транзакция БД с rollback при ошибках
- ✅ Подробное логирование всех операций

### Совместимость

Команда работает с ключами, созданными в любой версии бота, если:
- ✅ Ключ существует на Outline сервере
- ✅ Регион сервера указан в `Users.region_server` или используется дефолт `nederland`

---

## FAQ

**Q: Можно ли запускать миграцию несколько раз?**  
A: Да, команда автоматически пропустит уже мигрированных пользователей.

**Q: Что делать с пользователями, чьи ключи не найдены?**  
A: Выдайте им новые ключи через `/promo` или попросите купить снова.

**Q: Удалятся ли старые данные?**  
A: Нет, поле `Users.key` остается нетронутым для безопасности.

**Q: Как быстро работает миграция?**  
A: ~100-200 пользователей в минуту (зависит от скорости ответа Outline API).

**Q: Можно ли откатить миграцию?**  
A: Да, просто удалите записи из таблицы `UserKey` и восстановите БД из бэкапа.

---

## Мониторинг после миграции

После успешной миграции рекомендуется:

1. **Проверить работу бота:**
   - Команда `/start` показывает ключ
   - Кнопка "🔑 Мой ключ" работает
   - Платежи создают новые ключи в `UserKey`

2. **Проверить автоматическую блокировку:**
   - Подождите следующего цикла проверки (каждые 5 минут)
   - Убедитесь, что истекшие ключи блокируются

3. **Мониторить логи:**
   - Ищите ошибки связанные с ключами
   - Проверяйте успешность операций

---

## Поддержка

Если возникли проблемы при миграции:

1. Проверьте логи: `logs/log_main.py`
2. Найдите секцию `[MIGRATION]`
3. Создайте issue с описанием ошибки и логами
