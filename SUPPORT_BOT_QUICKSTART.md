# Быстрый старт бота техподдержки

## Что нужно перед запуском

### 1. Создайте бота техподдержки в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте команду: `/newbot`
3. Введите имя бота: `Your Support Bot`
4. Введите username: `your_support_bot` (должен заканчиваться на `bot`)
5. **Скопируйте токен** который даст BotFather

### 2. Добавьте токен в .env

Откройте файл `.env` (или `core/TEMP.env` для разработки) и добавьте:

```env
SUPPORT_BOT_TOKEN=ваш_токен_от_BotFather
```

Убедитесь, что также есть:
```env
ADMIN_TLG=ваш_telegram_id
```

### 3. Узнайте свой Telegram ID

Если не знаете свой ID:
1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Он отправит вам ваш ID
3. Вставьте его в `ADMIN_TLG`

## Запуск бота

### Вариант 1: Прямой запуск

```bash
# Из корневой директории проекта
python support_bot.py
```

### Вариант 2: В фоне (Linux)

```bash
# Запуск в фоне с логами
nohup python support_bot.py > support_bot.log 2>&1 &

# Проверка запуска
tail -f support_bot.log
```

## Проверка работы

### 1. Проверьте уведомление администратору

После запуска вы должны получить сообщение от бота:

```
🟢 Бот техподдержки запущен

🤖 Бот: @your_support_bot
🆔 Bot ID: 123456789
✅ Статус: Токен получен успешно
🔄 Состояние: Готов к приёму сообщений
```

### 2. Протестируйте с другого аккаунта

1. Откройте бота с другого Telegram аккаунта
2. Отправьте команду `/start`
3. Напишите тестовое сообщение
4. Проверьте, что вы (администратор) получили это сообщение

### 3. Ответьте пользователю

**Способ 1 (Рекомендуется):**
- Нажмите "Ответить" (Reply) на сообщение пользователя
- Напишите ответ
- Отправьте

**Способ 2:**
```
/reply USER_ID текст ответа
```

## Что означают сообщения при запуске

### ✅ Успешный запуск

```
==================================================
Запуск бота техподдержки...
Токен бота: ✓ Установлен
ID администратора: 123456789
==================================================
INFO - Бот техподдержки запущен
```

### ❌ Ошибка: токен не найден

```
RuntimeError: Отсутствует переменная окружения SUPPORT_BOT_TOKEN
```

**Решение:** Добавьте `SUPPORT_BOT_TOKEN` в `.env` файл

### ❌ Ошибка: неверный токен

```
ERROR - Ошибка при получении информации о боте
```

**Решение:** Проверьте правильность токена от BotFather

### ❌ Ошибка: нет ID администратора

```
RuntimeError: Отсутствует переменная окружения ADMIN_TLG
```

**Решение:** Добавьте `ADMIN_TLG` в `.env` файл

## Остановка бота

### Вариант 1: Ctrl+C

```bash
# Нажмите Ctrl+C в терминале
```

Вы получите уведомление:
```
🔴 Бот техподдержки остановлен
⏹️ Статус: Бот прекратил работу
```

### Вариант 2: Из фона

```bash
# Найдите процесс
ps aux | grep support_bot.py

# Остановите
kill PID_процесса
```

## Типичные проблемы

### Бот не отвечает на сообщения

1. **Проверьте:** Бот запущен?
   ```bash
   ps aux | grep support_bot.py
   ```

2. **Проверьте:** Логи
   ```bash
   tail -f support_bot.log
   ```

3. **Проверьте:** Токен корректный?
   - Попробуйте создать нового бота
   - Обновите токен

### Не приходят уведомления администратору

1. **Проверьте:** ID администратора правильный?
   ```bash
   echo $ADMIN_TLG
   ```

2. **Проверьте:** Вы начали диалог с ботом?
   - Откройте бота
   - Нажмите START

### Администратор не может ответить

1. **Проверьте:** Вы используете Reply на сообщение пользователя?
2. **Или:** Используете команду `/reply USER_ID текст`
3. **Проверьте:** Вы отвечаете именно тем аккаунтом, который указан в `ADMIN_TLG`?

## Дальнейшие шаги

После успешного запуска:

1. **Создайте systemd сервис** для автозапуска (см. `olvpn-support-bot.service`)
2. **Настройте логирование** в отдельный файл
3. **Изучите** полную документацию в `SUPPORT_BOT_IMPROVEMENTS.md`

## Контакты

При возникновении проблем:
- Проверьте логи: `support_bot.log`
- Изучите документацию: `SUPPORT_BOT_IMPROVEMENTS.md`
- Проверьте `.env` файл

---

**Версия:** 2.0  
**Дата:** 20 декабря 2025
