## Новая команда администратора: /addserver

### Описание
Команда `/addserver` позволяет администратору добавить новый VPN-сервер в интерактивном диалоговом режиме. После добавления сервер автоматически становится доступным для пользователей при покупке ключей.

Также можно указать максимальное количество ключей для сервера. Если лимит достигнут, сервер становится недоступным для новых покупок.

### Использование

```
/addserver
```

Команда доступна только администратору (ID указан в переменной окружения `ADMIN_TLG`).

### Этапы диалога

**Этап 1: Название страны**
```
🌍 Добавление нового VPN сервера

Введите название страны (например: Нидерланды, США, Германия)
```
Администратор вводит название (например: "Нидерланды")
→ Флаг выбирается автоматически (🇳🇱)

**Этап 2: API URL**
```
🇳🇱 Нидерланды

Теперь введите API URL сервера Outline
(пример: https://ip:port или https://example.com:port)
```

**Этап 3: SHA256 сертификата**
```
Теперь введите SHA256 сертификата сервера
(строка вида: aabbccdd...)
```

**Этап 4: Максимальное количество ключей**
```
Введите максимальное количество ключей для этого сервера
(пример: 100 или 500)
```

**Этап 5: Подтверждение**
```
✅ Проверьте данные сервера:

🇳🇱 Страна: Нидерланды
API URL: https://192.168.1.1:8443
SHA256: aabbccdd1122...
Макс. ключей: 100

Добавить сервер? (Введите да для подтверждения или нет для отмены)
```

### Что происходит после добавления

1. **Сохранение в конфиг**
   - Сервер добавляется в файл `core/api_s/outline/settings_api_outline.json`
   - Автоматически генерируется ключ сервера (lowercase, с подчёркиваниями вместо пробелов)
   - Сохраняется максимальное количество ключей (`max_keys`)

2. **Доступность для пользователей**
   - Сервер сразу появляется в меню выбора региона при покупке ключа
   - Пользователи могут выбрать этот регион и купить ключ
   - **При достижении лимита ключей** сервер становится недоступным до удаления некоторых ключей

3. **Структура добавленного сервера в JSON**
   ```json
   {
     "netherlands": {
       "name_en": "netherlands",
       "name_ru": "🇳🇱 Нидерланды",
       "api_url": "https://ip:port",
       "cert_sha256": "aabbccdd...",
       "max_keys": 100,
       "is_active": true
     }
   }
   ```

### Проверка лимита ключей

Когда пользователь выбирает длительность ключа (1 день/неделя/месяц):
- Система автоматически проверяет текущее количество ключей на Outline сервере
- Если `текущие ключи >= max_keys`, сервер **недоступен для покупки**
- Пользователю выводится сообщение:
  ```
  ❌ Сервер недоступен

  Достигнут лимит ключей (100/100)
  Пожалуйста, выберите другой регион
  ```

### Автоматический выбор флагов

Флаги выбираются автоматически на основе названия страны. Поддерживаются:
- Англоязычные названия: Netherlands → 🇳🇱, USA → 🇺🇸, Germany → 🇩🇪
- Русскоязычные названия: Нидерланды → 🇳🇱, США → 🇺🇸
- И многие другие страны (см. `COUNTRY_FLAGS` в `core/utils/server_config.py`)

Если флаг не найден, используется стандартный 🌍

### Технические детали

**Файлы:**
- `core/handlers/add_server.py` - обработчик команды и FSM логика
- `core/utils/server_config.py` - утилиты для работы с флагами, конфигом и проверкой лимитов
- `core/api_s/outline/settings_api_outline.json` - конфигурационный файл с серверами
- `core/bot.py` - регистрация команды и FSM состояний

**FSM Состояния:**
- `AddServerStates.waiting_for_country_name` - ввод названия страны
- `AddServerStates.waiting_for_api_url` - ввод API URL
- `AddServerStates.waiting_for_cert_sha256` - ввод SHA256
- `AddServerStates.waiting_for_max_keys` - ввод максимального количества ключей
- `AddServerStates.confirming_server` - подтверждение добавления

**Проверки при добавлении:**
- Сервер не должен существовать (проверяется по ключу)
- API URL должен начинаться с `https://`
- SHA256 должен иметь минимальную длину (10 символов)
- Название страны не должно быть пусто
- Максимальное количество ключей должно быть числом > 0

### Пример полного диалога

```
Пользователь: /addserver
Бот: 🌍 Добавление нового VPN сервера
     Введите название страны...

Пользователь: Франция
Бот: 🇫🇷 Франция
     Введите API URL...

Пользователь: https://paris.vpn.com:8443
Бот: Введите SHA256 сертификата...

Пользователь: 1a2b3c4d5e6f7g8h9i0j
Бот: Введите максимальное количество ключей...

Пользователь: 150
Бот: ✅ Проверьте данные сервера:
     🇫🇷 Страна: Франция
     API URL: https://paris.vpn.com:8443
     SHA256: 1a2b3c4d5e...
     Макс. ключей: 150
     Добавить сервер?

Пользователь: да
Бот: ✅ Сервер успешно добавлен!
     🇫🇷 Франция
     Макс. ключей: 150
     Сервер будет доступен пользователям при выборе региона.
```

### Обработка ошибок

**"Сервер с таким названием уже существует"**
- Попробуйте вариант названия (например, "South Korea" вместо "Korea")

**"API URL должен начинаться с https://"**
- Убедитесь, что URL имеет вид: `https://ip:port` или `https://domain:port`

**"SHA256 сертификата должен быть корректным"**
- Скопируйте полный SHA256 сертификата от администратора сервера

**"Количество ключей должно быть больше 0"**
- Введите положительное число

### Расширение списка флагов

Для добавления новых стран отредактируйте словарь `COUNTRY_FLAGS` в `core/utils/server_config.py`:

```python
COUNTRY_FLAGS = {
    "ваша_страна": "🏳️",  # Замените на правильный флаг
    # ...
}
```

Тогда команда автоматически будет использовать новый флаг.

### Изменение лимита уже существующего сервера

Отредактируйте поле `max_keys` в файле `core/api_s/outline/settings_api_outline.json`:

```json
{
  "netherlands": {
    ...
    "max_keys": 200
  }
}
```

Изменение вступит в силу после перезагрузки бота.
